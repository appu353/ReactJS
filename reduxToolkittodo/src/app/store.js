import { configureStore } from "@reduxjs/toolkit";

export const store = configureStore({}); //configureStore will always accept obj
//Step for creating store:=
//import { configureStore } from "@reduxjs/toolkit";
